name: 'car_data_platform'
version: '1.0.0'
config-version: 2

profile: 'car_data_platform'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  car_data_platform:
    +materialized: view

    staging:
      +schema: staging

    marts:
      +schema: marts